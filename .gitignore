# Credenciales — nunca subir al repo
.env

# Certificado corporativo (específico del entorno)
# Regenerar con: Export-Certificate -Cert (Get-ChildItem Cert:\LocalMachine\Root | Where Subject -like "*Zscaler*") -FilePath zscaler-ca.pem
zscaler-ca.pem

# Tokens OAuth de Garmin (guardados localmente por el MCP)
.garmin-mcp/

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/
*.egg-info/
dist/
build/
.pytest_cache/

# Node
node_modules/

# VS Code
.vscode/settings.json

# Memoria del usuario (puede contener datos personales)
memory/user_profile.json
memory/session_context.json
memory/gemini_daily_usage.json

# OS
.DS_Store
Thumbs.db
